name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ] # change to the default branch name for your fork if different

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: node build

      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v1
        with:
          path: 'dist' # make sure `node build` outputs to dist/

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1